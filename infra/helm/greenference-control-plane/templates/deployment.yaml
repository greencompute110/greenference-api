apiVersion: apps/v1
kind: Deployment
metadata:
  name: greencompute-control-plane
spec:
  replicas: 1
  selector:
    matchLabels:
      app: greencompute-control-plane
  template:
    metadata:
      labels:
        app: greencompute-control-plane
    spec:
      containers:
        - name: control-plane
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 8000
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 5

